name: neurofibroma_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.7
  flutter_pytorch_lite: ^0.1.0+3
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.2
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/models/nf_cls_ts.ptl
    - assets/models/labels.txt

